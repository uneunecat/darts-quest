## DARTS QUEST 開発引き継ぎドキュメント

1. プロジェクトのアイデンティティ
- 本作は**「実際のダーツ（物理動作）」と「ファンタジーRPG」を融合させたUX重視のゲーム**です。
- 核心的UX: ダーツを「投げる」楽しさと、刺さったダーツを「抜く」時間をドロー（思考時間）としてゲームサイクルに組み込んだ「インターバル・システム」。

2. 3つの技術的柱（アーキテクチャ）
新しいAIが最も尊重すべきエンジンの設計思想です。

① アトミック・スキル・エンジン (Async/Await Sequence)
- 全ての行動（敵の技、プレイヤーのカード、トラップ）は、**「アトム（最小単位の効果）」**の配列として定義されています。
- 鉄則: 演出（SE、振動、カットイン、ダメージ）は必ず async/await で直列に管理すること。
- 理由: 演出が重なってログが飛び出すのを防ぎ、プレイヤーが「何が起きたか」を理解する「間（余韻）」をデータ側からミリ秒単位で制御するため。

② ユニバーサル・ステート・システム (v5.0)
- 状態異常（バフ・デバフ）は個別変数ではなく、states: [] 配列で一元管理されています。
- 計算ロジック: 攻撃・防御関数がこの配列をスキャンし、category（atk_mult等）に基づいて自動で補正をかけます。
- タイミング: timing: "throw"（投げるたびに減少）と timing: "round"（敵ターン終了時に減少）の2系統が厳密に区別されています。

③ デザインシステム：標準カード形式
- カードの描画は ui.js の createCardElement に集約されています。
- 技術: CSSの container-query (cqw) と mask-composite を使用。
- 利点: サイズ（small, standard, battle）を指定するだけで、枠の太さや文字サイズが動的に最適化されます。

3. 物理法則（計算式の統一）
- ダメージ計算は、以下の2つの共通関数以外で行ってはなりません。
- applyOffenseLogic: 攻撃力、バフ（倍率）、固定加算、乱数を一括計算。
- applyDefenseLogic: 結界（ダメージ閾値）、防御（割合）、アーマー（固定値）を一括計算。

4. データ構造のルール (data.js)
- WORLD_MAP: Area > Stage > Floor の階層。ステージIDは "1-1" などの文字列。
- TIMING: 全ての演出秒数はマジックナンバーを使わず、TIMING 定数から参照すること。

5. 現在の懸念事項と「絶対に避けるべきこと」
- デグレード（品質退行）の防止: ロジックを綺麗にしようとして、async/await を setTimeout に戻したり、共通化された計算式を各関数に分散させたりしないでください。
- 1Fのタイトル重なり: ステージ開始時のチャプター表示と敵出現演出のタイミングは、プリロード完了を待ってから連鎖する「イベント駆動」になっています。

6. 今後の開発ロードマップ
- 新しい環境で優先的に取り組むべきタスクです。
- リサイクルシステム: 4枚目以降の重複カードを「ソウル」に変換し、アイテムや未所持カードと交換する経済圏の構築。
- ステートの完全配列化: itemLockTurn などの残存している個別フラグを、すべて states: [] 配列へ完全移行する。
- AREA 2 / 3 の拡張: WORLD_MAP へのデータ追加による新ステージの実装。